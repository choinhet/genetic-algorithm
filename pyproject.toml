[project]
name = "genetic-algorithm"
version = "0.1.0"
description = "This package provides a generic class so you can work on simple GAs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3.1",
    "pandas>=2.3.0",
    "pydantic>=2.11.7",
]
